steps:

- task: AzurePowerShell@3
  displayName: 'Azure PowerShell script: InlineScript'
  inputs:
    azureSubscription: ram
    ScriptType: InlineScript
    Inline: '$env:test'
    azurePowerShellVersion: LatestVersion
